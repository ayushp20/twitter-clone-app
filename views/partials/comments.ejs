<% if (tweet.comments.length > 0) {%>
<div class="col-md-6 m-auto">
      <div class="card card-body text-center">
                  <% tweet.comments.forEach(comment =>{  %>
                        <div class="card text-white bg-success mb-3">
                              <div class="card-header"><%= comment.creator.name %></div>
                              <div class="card-body">
                                    <h4 class="card-title"><%= comment.message%></h4>
                                    <p class="card-text"><%= comment.dateCreated.toDateString() %> <%= comment.dateCreated.toTimeString().split(" ",1)%></p>
                              </div>
                        </div>
                  <% }) %> 

      </div>
</div>
<% }else{ %>
<h3>No Comments for this tweet.</h3>
<% } %>